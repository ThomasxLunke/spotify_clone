import AuthForm from "../components/authForm";
// auto generate with the command : rafce

const Signin = () => {
  return (
    <AuthForm mode="signin"/>
  )
}

Signin.authPage = true

export default Signin